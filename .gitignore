# === Python ===
__pycache__/
*.pyc
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# === Editores y SO ===
.vscode/
.idea/
.DS_Store
Thumbs.db

# === Herramientas y agentes locales ===
.claude/
.worktrees/

# === Datos crudos MTE (licencia restringida, no redistribuir) ===
MedicionesMTE/
MedicionesMTE_v3/

# === Documentos: material de tesis / notas / normativa — LOCAL, no publico (2026-07) ===
# El repo publico deja solo la estructura de codigo. Toda la carpeta Documentos/
# (propuesta, notas, bibliografia y copias de normativa CREG) se conserva local.
Documentos/

# === Outputs telemetria Sprint 5.1 (D3) ===
outputs/telemetry/

# === Outputs paper Sprint 6.1 (CAL-25) ===
# Mantenemos solo el xlsx final del paper; los logs y figuras intermedias
# se gitignoran salvo si el autor las mueve al repo manualmente.
outputs/paper/log_*.txt
outputs/paper/.cache/
outputs/paper/intermediate_*.csv

# === Resultados y artefactos de corrida ===
*.xlsx
*.log
# Pattern outputs/* (no outputs/) para permitir excepciones via !outputs/paper/...
outputs/*
!outputs/paper/

# === Paper WEEF: fuente md + BibTeX — LOCAL, no publico (2026-07) ===
# El paper es entregable, pero se retira del repo publico por decision del autor.
# Se conserva local junto con el resto de outputs/paper/ (regenerable).
outputs/paper/*
p2p_breakdown_flujos.csv
p2p_breakdown_resumen_horario.csv

# === Backups defensivos creados por planes de auditoria ===
_backup_*/

# === Runtime local de agentes ===
.claude-flow/
.swarm/
.mcp.json
.claude.bak/
CLAUDE.md.bak
ruvector.db
*.db

# === Ruflo runtime extra (instalación local) ===
agentdb.rvf
agentdb.rvf.lock

# === Capturas de reunión (binarios pesados, valor solo a corto plazo) ===
comp/

# === Backups defensivos de CSV (regenerables vía pydataxm) ===
data/*.bak_*
data/*.csv.bak_*

# Presentación generada
presentation/*.pptx

# === Docs meta y de avance — LOCAL, no publico (2026-07) ===
# CLAUDE.md (instrucciones de proceso/IA), SERVER_SETUP.md (despliegue interno)
# y REPORTE_AVANCES.md (bitacora) se conservan localmente, fuera del repo publico.
/CLAUDE.md
/SERVER_SETUP.md
/REPORTE_AVANCES.md

# === Transcripciones y material privado de reuniones (repo público: NO subir) ===
Documentos/reuniones/

# === Outputs traídos del servidor (regenerables) ===
SALIDAS_SERVIDOR/

# === Material divulgativo local + scratch ===
fanzine/
.superpowers/
borrar.py

# === Proceso asistido por IA / ingenieria interna (repo publico: NO subir) ===
docs/
scripts/export_ruflo_memory.py
scripts/ruflo_index_mte_profiles.py
scripts/ruflo_snapshot_run.py
scripts/backup_notas_tesis.py
tests/test_ruflo_snapshot_run.py
tests/test_backup_notas.py
tests/test_mte_profiles_indexer.py

# === Datos MTE derivados (licencia restringida, no redistribuir) ===
graficas/*.csv
graficas/*.mat
